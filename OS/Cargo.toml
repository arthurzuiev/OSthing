[package]
name = "OS"
version = "0.1.0"
edition = "2024"

[dependencies]
bootloader = "0.9"

# to make rust-analyzer happy :D
[[bin]]
name = "OS"
test = false
bench = false

# panic is not an option in BIOS wastelands...
[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
